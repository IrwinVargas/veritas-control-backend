AWSTemplateFormatVersion: '2010-09-09'
Transform: AWS::Serverless-2016-10-31 # Activa SAM
Description: 'Backend Serverless de Veritas Control optimizado con AWS SAM'

Parameters:
  Environment:
    Type: String
    Default: dev
    AllowedValues: [dev, qa, prod]
  CognitoUserPoolArn:
    Type: String
  RdsSecretArnFromSsm:
    Type: 'AWS::SSM::Parameter::Value<String>'
    Default: '/veritas-control/dev/rds/postgres-secret-arn'

Globals:
  Function:
    Timeout: 90
    MemorySize: 512
    Runtime: python3.11
    Architectures:
      - x86_64

Resources:
  # API GATEWAY CENTRAL CON SWAGGER INTEGRADO
  VeritasApi:
    Type: AWS::Serverless::Api
    Properties:
      StageName: !Ref Environment
      DefinitionBody:
        openapi: "3.0.1"
        info:
          title: "Veritas Control - API de Gestión Jurídica"
          version: "1.0.0"

        components:
          securitySchemes:
            CognitoAuthorizer:
              type: "apiKey"
              name: "Authorization"
              in: "header"
              x-amazon-apigateway-authtype: "cognito_user_pools"
              x-amazon-apigateway-authorizer:
                type: "cognito_user_pools"
                providerARNs:
                  - !Ref CognitoUserPoolArn
        paths:
          /swagger-json:
            get:
              summary: "Descargar esquema OpenAPI de Veritas Control"
              responses:
                '200':
                  description: "Estructura JSON de Swagger exitosa."
              # Retorna un mock de la especificación técnica de forma directa
              x-amazon-apigateway-integration:
                type: "mock"
                requestTemplates:
                  application/json: "{\"statusCode\": 200}"
                responses:
                  default:
                    statusCode: "200"
                    responseTemplates:
                      application/json: "{\"info\": {\"title\": \"Veritas Control\", \"version\": \"1.0.0\"}, \"paths\": {}}"
          /registro:
            post:
              summary: "Alta Administrativa de Personal (Solo Socios)"
              security:
                - CognitoAuthorizer: []
              x-amazon-apigateway-integration:
                type: "aws_proxy"
                httpMethod: "POST"
                uri: !Sub 'arn:aws:apigateway:${AWS::Region}:lambda:path/2015-03-31/functions/${RegistroLambda.Arn}/invocations'
                passthroughBehavior: "when_no_match"
              responses:
                '201':
                  description: "Usuario registrado con éxito."

            options:
              summary: "Soporte de CORS Preflight"
              responses:
                '200':
                  description: "CORS Preflight exitoso"
                  headers:
                    Access-Control-Allow-Origin:
                      schema:
                        type: "string"
                    Access-Control-Allow-Methods:
                      schema:
                        type: "string"
                    Access-Control-Allow-Headers:
                      schema:
                        type: "string"
              x-amazon-apigateway-integration:
                type: "mock"
                requestTemplates:
                  application/json: "{\"statusCode\": 200}"
                responses:
                  default:
                    statusCode: "200"
                    responseParameters:
                      method.response.header.Access-Control-Allow-Headers: "'Content-Type,Authorization,X-Amz-Date,X-Api-Key,X-Amz-Security-Token'"
                      method.response.header.Access-Control-Allow-Methods: "'POST,OPTIONS'"
                      method.response.header.Access-Control-Allow-Origin: "'*'"
                    responseTemplates:
                      application/json: ""
          /finanzas/cargar-excel:
            post:
              summary: "Subir archivo Excel del SAT y vaciar datos a Postgres"
              security:
                - CognitoAuthorizer: []
              x-amazon-apigateway-integration:
                type: "aws_proxy"
                httpMethod: "POST"
                uri: !Sub 'arn:aws:apigateway:${AWS::Region}:lambda:path/2015-03-31/functions/${CargaExcelLambda.Arn}/invocations'
                passthroughBehavior: "when_no_match"
              responses:
                '200':
                  description: "Excel cargado e indexado de forma correcta."
            options:
              summary: "CORS Cargar Excel"
              responses:
                '200':
                  description: "CORS Preflight exitoso"
                  headers:
                    Access-Control-Allow-Origin: { schema: { type: "string" } }
                    Access-Control-Allow-Methods: { schema: { type: "string" } }
                    Access-Control-Allow-Headers: { schema: { type: "string" } }
              x-amazon-apigateway-integration:
                type: "mock"
                requestTemplates:
                  application/json: "{\"statusCode\": 200}"
                responses:
                  default:
                    statusCode: "200"
                    responseParameters:
                      method.response.header.Access-Control-Allow-Headers: "'Content-Type,Authorization,X-Amz-Date,X-Api-Key,X-Amz-Security-Token'"
                      method.response.header.Access-Control-Allow-Methods: "'POST,OPTIONS'"
                      method.response.header.Access-Control-Allow-Origin: "'*'"
                    responseTemplates:
                      application/json: ""

          /finanzas/dashboard-metricas:
            get:
              summary: "Obtener calculos macro para las tarjetas y graficas"
              security:
                - CognitoAuthorizer: []
              x-amazon-apigateway-integration:
                type: "aws_proxy"
                httpMethod: "POST" # API Gateway invoca a Lambda internamente como POST siempre
                uri: !Sub 'arn:aws:apigateway:${AWS::Region}:lambda:path/2015-03-31/functions/${MetricasLambda.Arn}/invocations'
                passthroughBehavior: "when_no_match"
              responses:
                '200':
                  description: "Métricas consolidadas calculadas con éxito."
            options:
              summary: "CORS Metricas"
              responses:
                '200':
                  description: "CORS Preflight exitoso"
                  headers:
                    Access-Control-Allow-Origin: { schema: { type: "string" } }
                    Access-Control-Allow-Methods: { schema: { type: "string" } }
                    Access-Control-Allow-Headers: { schema: { type: "string" } }
              x-amazon-apigateway-integration:
                type: "mock"
                requestTemplates:
                  application/json: "{\"statusCode\": 200}"
                responses:
                  default:
                    statusCode: "200"
                    responseParameters:
                      method.response.header.Access-Control-Allow-Headers: "'Content-Type,Authorization,X-Amz-Date,X-Api-Key,X-Amz-Security-Token'"
                      method.response.header.Access-Control-Allow-Methods: "'GET,OPTIONS'"
                      method.response.header.Access-Control-Allow-Origin: "'*'"
                    responseTemplates:
                      application/json: ""

          /finanzas/url-firmada:
            get:
              summary: "Solicitar pase de subida seguro directo a S3"
              security:
                - CognitoAuthorizer: []
              x-amazon-apigateway-integration:
                type: "aws_proxy"
                httpMethod: "POST"
                uri: !Sub 'arn:aws:apigateway:${AWS::Region}:lambda:path/2015-03-31/functions/${GenerarUrlFirmadaLambda.Arn}/invocations'
                passthroughBehavior: "when_no_match"
              responses:
                '200':
                  description: "Métricas consolidadas calculadas con éxito."
            options:
              summary: "CORS Metricas"
              responses:
                '200':
                  description: "CORS Preflight exitoso"
                  headers:
                    Access-Control-Allow-Origin: { schema: { type: "string" } }
                    Access-Control-Allow-Methods: { schema: { type: "string" } }
                    Access-Control-Allow-Headers: { schema: { type: "string" } }
              x-amazon-apigateway-integration:
                type: "mock"
                requestTemplates:
                  application/json: "{\"statusCode\": 200}"
                responses:
                  default:
                    statusCode: "200"
                    responseParameters:
                      method.response.header.Access-Control-Allow-Headers: "'Content-Type,Authorization,X-Amz-Date,X-Api-Key,X-Amz-Security-Token'"
                      method.response.header.Access-Control-Allow-Methods: "'GET,OPTIONS'"
                      method.response.header.Access-Control-Allow-Origin: "'*'"
                    responseTemplates:
                      application/json: ""
          /finanzas/clientes:
            get:
              summary: "Obtener catálogo único de clientes"
              security: 
                - CognitoAuthorizer: []
              x-amazon-apigateway-integration:
                type: "aws_proxy"
                httpMethod: "POST"
                uri: !Sub 'arn:aws:apigateway:${AWS::Region}:lambda:path/2015-03-31/functions/${ListaClientesLambda.Arn}/invocations'
                passthroughBehavior: "when_no_match"
              responses:
                '200': { description: "Éxito" }
            options:
              summary: "CORS Metricas"
              consumes:
                - "application/json"
              produces:
                - "application/json"
              responses:
                '200':
                  description: "CORS Preflight exitoso"
                  headers:
                    Access-Control-Allow-Headers:
                      type: "string"
                    Access-Control-Allow-Methods:
                      type: "string"
                    Access-Control-Allow-Origin:
                      type: "string"
              x-amazon-apigateway-integration:
                type: "mock"
                requestTemplates:
                  application/json: "{\"statusCode\": 200}"
                responses:
                  default:
                    statusCode: "200"
                    responseParameters:
                      method.response.header.Access-Control-Allow-Headers: "'Content-Type,Authorization,X-Amz-Date,X-Api-Key,X-Amz-Security-Token'"
                      method.response.header.Access-Control-Allow-Methods: "'GET,OPTIONS'"
                      method.response.header.Access-Control-Allow-Origin: "'*'"
                    responseTemplates:
                      application/json: ""
                passthroughBehavior: "when_no_match"

          /materialidad/generar:
            post:
              summary: "Encender la Step Function pericial en segundo plano"
              security:
                - CognitoAuthorizer: []
              x-amazon-apigateway-integration:
                type: "aws_proxy"
                httpMethod: "POST"
                uri: !Sub 'arn:aws:apigateway:${AWS::Region}:lambda:path/2015-03-31/functions/${MaterialidadDisparadorLambda.Arn}/invocations'
                passthroughBehavior: "when_no_match"
              responses:
                '202': { description: "Orquestador encendido con éxito." }
            options:
              summary: "CORS Metricas"
              consumes:
                - "application/json"
              produces:
                - "application/json"
              responses:
                '200':
                  description: "CORS Preflight exitoso"
                  headers:
                    Access-Control-Allow-Headers:
                      type: "string"
                    Access-Control-Allow-Methods:
                      type: "string"
                    Access-Control-Allow-Origin:
                      type: "string"
              x-amazon-apigateway-integration:
                type: "mock"
                requestTemplates:
                  application/json: "{\"statusCode\": 200}"
                responses:
                  default:
                    statusCode: "200"
                    responseParameters:
                      method.response.header.Access-Control-Allow-Headers: "'Content-Type,Authorization,X-Amz-Date,X-Api-Key,X-Amz-Security-Token'"
                      method.response.header.Access-Control-Allow-Methods: "'POST,OPTIONS'"
                      method.response.header.Access-Control-Allow-Origin: "'*'"
                    responseTemplates:
                      application/json: ""
                passthroughBehavior: "when_no_match"

          /materialidad/expediente:
            get:
              summary: "Consultar estatus de PDFs y descargar URLs firmadas de S3"
              security:
                - CognitoAuthorizer: []
              x-amazon-apigateway-integration:
                type: "aws_proxy"
                httpMethod: "POST"
                uri: !Sub 'arn:aws:apigateway:${AWS::Region}:lambda:path/2015-03-31/functions/${MaterialidadListarArchivosLambda.Arn}/invocations'
                passthroughBehavior: "when_no_match"
              responses:
                '200': { description: "Estatus recuperado en 2ms." }
            options:
              summary: "CORS Metricas"
              consumes:
                - "application/json"
              produces:
                - "application/json"
              responses:
                '200':
                  description: "CORS Preflight exitoso"
                  headers:
                    Access-Control-Allow-Headers:
                      type: "string"
                    Access-Control-Allow-Methods:
                      type: "string"
                    Access-Control-Allow-Origin:
                      type: "string"
              x-amazon-apigateway-integration:
                type: "mock"
                requestTemplates:
                  application/json: "{\"statusCode\": 200}"
                responses:
                  default:
                    statusCode: "200"
                    responseParameters:
                      method.response.header.Access-Control-Allow-Headers: "'Content-Type,Authorization,X-Amz-Date,X-Api-Key,X-Amz-Security-Token'"
                      method.response.header.Access-Control-Allow-Methods: "'GET,OPTIONS'"
                      method.response.header.Access-Control-Allow-Origin: "'*'"
                    responseTemplates:
                      application/json: ""
                passthroughBehavior: "when_no_match"

          /materialidad/preventivo/contrato:
            post:
              summary: "Encender la Step Function pericial en segundo plano"
              security:
                - CognitoAuthorizer: []
              x-amazon-apigateway-integration:
                type: "aws_proxy"
                httpMethod: "POST"
                uri: !Sub 'arn:aws:apigateway:${AWS::Region}:lambda:path/2015-03-31/functions/${MaterialidadPreventivoContratoLambda.Arn}/invocations'
                passthroughBehavior: "when_no_match"
              responses:
                '202': { description: "Orquestador encendido con éxito." }
            options:
              summary: "CORS Metricas"
              consumes:
                - "application/json"
              produces:
                - "application/json"
              responses:
                '200':
                  description: "CORS Preflight exitoso"
                  headers:
                    Access-Control-Allow-Headers:
                      type: "string"
                    Access-Control-Allow-Methods:
                      type: "string"
                    Access-Control-Allow-Origin:
                      type: "string"
              x-amazon-apigateway-integration:
                type: "mock"
                requestTemplates:
                  application/json: "{\"statusCode\": 200}"
                responses:
                  default:
                    statusCode: "200"
                    responseParameters:
                      method.response.header.Access-Control-Allow-Headers: "'Content-Type,Authorization,X-Amz-Date,X-Api-Key,X-Amz-Security-Token'"
                      method.response.header.Access-Control-Allow-Methods: "'POST,OPTIONS'"
                      method.response.header.Access-Control-Allow-Origin: "'*'"
                    responseTemplates:
                      application/json: ""
                passthroughBehavior: "when_no_match"

          /finanzas/auditoria-sat:
            get:
              summary: "Consultar lista negra del sat"
              security:
                - CognitoAuthorizer: []
              x-amazon-apigateway-integration:
                type: "aws_proxy"
                httpMethod: "POST"
                uri: !Sub 'arn:aws:apigateway:${AWS::Region}:lambda:path/2015-03-31/functions/${AuditoriaListaNegraLambda.Arn}/invocations'
                passthroughBehavior: "when_no_match"
              responses:
                '200': { description: "Estatus recuperado en 2ms." }
            options:
              summary: "CORS Metricas"
              consumes:
                - "application/json"
              produces:
                - "application/json"
              responses:
                '200':
                  description: "CORS Preflight exitoso"
                  headers:
                    Access-Control-Allow-Headers:
                      type: "string"
                    Access-Control-Allow-Methods:
                      type: "string"
                    Access-Control-Allow-Origin:
                      type: "string"
              x-amazon-apigateway-integration:
                type: "mock"
                requestTemplates:
                  application/json: "{\"statusCode\": 200}"
                responses:
                  default:
                    statusCode: "200"
                    responseParameters:
                      method.response.header.Access-Control-Allow-Headers: "'Content-Type,Authorization,X-Amz-Date,X-Api-Key,X-Amz-Security-Token'"
                      method.response.header.Access-Control-Allow-Methods: "'GET,OPTIONS'"
                      method.response.header.Access-Control-Allow-Origin: "'*'"
                    responseTemplates:
                      application/json: ""
                passthroughBehavior: "when_no_match"

          /materialidad/cliente:
            post:
              summary: "Crear cliente de manera manual"
              security:
                - CognitoAuthorizer: []
              x-amazon-apigateway-integration:
                type: "aws_proxy"
                httpMethod: "POST"
                uri: !Sub 'arn:aws:apigateway:${AWS::Region}:lambda:path/2015-03-31/functions/${CrearClienteManualLambda.Arn}/invocations'
                passthroughBehavior: "when_no_match"
              responses:
                '201': { description: "Cliente creado." }
            options:
              summary: "CORS Metricas"
              consumes:
                - "application/json"
              produces:
                - "application/json"
              responses:
                '200':
                  description: "CORS Preflight exitoso"
                  headers:
                    Access-Control-Allow-Headers:
                      type: "string"
                    Access-Control-Allow-Methods:
                      type: "string"
                    Access-Control-Allow-Origin:
                      type: "string"
              x-amazon-apigateway-integration:
                type: "mock"
                requestTemplates:
                  application/json: "{\"statusCode\": 200}"
                responses:
                  default:
                    statusCode: "200"
                    responseParameters:
                      method.response.header.Access-Control-Allow-Headers: "'Content-Type,Authorization,X-Amz-Date,X-Api-Key,X-Amz-Security-Token'"
                      method.response.header.Access-Control-Allow-Methods: "'POST,OPTIONS'"
                      method.response.header.Access-Control-Allow-Origin: "'*'"
                    responseTemplates:
                      application/json: ""
                passthroughBehavior: "when_no_match"

          /materialidad/preventivo/planeacion:
            post:
              summary: "Crear cliente de manera manual"
              security:
                - CognitoAuthorizer: []
              x-amazon-apigateway-integration:
                type: "aws_proxy"
                httpMethod: "POST"
                uri: !Sub 'arn:aws:apigateway:${AWS::Region}:lambda:path/2015-03-31/functions/${MaterialidadPreventivoPlaneacionLambda.Arn}/invocations'
                passthroughBehavior: "when_no_match"
              responses:
                '201': { description: "Cliente creado." }
            options:
              summary: "CORS Metricas"
              consumes:
                - "application/json"
              produces:
                - "application/json"
              responses:
                '200':
                  description: "CORS Preflight exitoso"
                  headers:
                    Access-Control-Allow-Headers:
                      type: "string"
                    Access-Control-Allow-Methods:
                      type: "string"
                    Access-Control-Allow-Origin:
                      type: "string"
              x-amazon-apigateway-integration:
                type: "mock"
                requestTemplates:
                  application/json: "{\"statusCode\": 200}"
                responses:
                  default:
                    statusCode: "200"
                    responseParameters:
                      method.response.header.Access-Control-Allow-Headers: "'Content-Type,Authorization,X-Amz-Date,X-Api-Key,X-Amz-Security-Token'"
                      method.response.header.Access-Control-Allow-Methods: "'POST,OPTIONS'"
                      method.response.header.Access-Control-Allow-Origin: "'*'"
                    responseTemplates:
                      application/json: ""
                passthroughBehavior: "when_no_match"
          /materialidad/preventivo/bimestral:
            post:
              summary: "Crear cliente de manera manual"
              security:
                - CognitoAuthorizer: []
              x-amazon-apigateway-integration:
                type: "aws_proxy"
                httpMethod: "POST"
                uri: !Sub 'arn:aws:apigateway:${AWS::Region}:lambda:path/2015-03-31/functions/${MaterialidadPreventivoBimestralLambda.Arn}/invocations'
                passthroughBehavior: "when_no_match"
              responses:
                '201': { description: "Cliente creado." }
            options:
              summary: "CORS Metricas"
              consumes:
                - "application/json"
              produces:
                - "application/json"
              responses:
                '200':
                  description: "CORS Preflight exitoso"
                  headers:
                    Access-Control-Allow-Headers:
                      type: "string"
                    Access-Control-Allow-Methods:
                      type: "string"
                    Access-Control-Allow-Origin:
                      type: "string"
              x-amazon-apigateway-integration:
                type: "mock"
                requestTemplates:
                  application/json: "{\"statusCode\": 200}"
                responses:
                  default:
                    statusCode: "200"
                    responseParameters:
                      method.response.header.Access-Control-Allow-Headers: "'Content-Type,Authorization,X-Amz-Date,X-Api-Key,X-Amz-Security-Token'"
                      method.response.header.Access-Control-Allow-Methods: "'POST,OPTIONS'"
                      method.response.header.Access-Control-Allow-Origin: "'*'"
                    responseTemplates:
                      application/json: ""
                passthroughBehavior: "when_no_match"
          /materialidad/cliente/producto:
            post:
              summary: "Crear cliente de manera manual"
              security:
                - CognitoAuthorizer: []
              x-amazon-apigateway-integration:
                type: "aws_proxy"
                httpMethod: "POST"
                uri: !Sub 'arn:aws:apigateway:${AWS::Region}:lambda:path/2015-03-31/functions/${MaterialidadPreventivoBimestralLambda.Arn}/invocations'
                passthroughBehavior: "when_no_match"
              responses:
                '201': { description: "Cliente creado." }
            options:
              summary: "CORS Metricas"
              consumes:
                - "application/json"
              produces:
                - "application/json"
              responses:
                '200':
                  description: "CORS Preflight exitoso"
                  headers:
                    Access-Control-Allow-Headers:
                      type: "string"
                    Access-Control-Allow-Methods:
                      type: "string"
                    Access-Control-Allow-Origin:
                      type: "string"
              x-amazon-apigateway-integration:
                type: "mock"
                requestTemplates:
                  application/json: "{\"statusCode\": 200}"
                responses:
                  default:
                    statusCode: "200"
                    responseParameters:
                      method.response.header.Access-Control-Allow-Headers: "'Content-Type,Authorization,X-Amz-Date,X-Api-Key,X-Amz-Security-Token'"
                      method.response.header.Access-Control-Allow-Methods: "'POST,OPTIONS'"
                      method.response.header.Access-Control-Allow-Origin: "'*'"
                    responseTemplates:
                      application/json: ""
                passthroughBehavior: "when_no_match"

          /materialidad/core/orquestar:
            post:
              summary: "Crear cliente de manera manual"
              security:
                - CognitoAuthorizer: []
              x-amazon-apigateway-integration:
                type: "aws_proxy"
                httpMethod: "POST"
                uri: !Sub 'arn:aws:apigateway:${AWS::Region}:lambda:path/2015-03-31/functions/${MaterialidadOrquestadorLambda.Arn}/invocations'
                passthroughBehavior: "when_no_match"
              responses:
                '201': { description: "Cliente creado." }
            options:
              summary: "CORS Metricas"
              consumes:
                - "application/json"
              produces:
                - "application/json"
              responses:
                '200':
                  description: "CORS Preflight exitoso"
                  headers:
                    Access-Control-Allow-Headers:
                      type: "string"
                    Access-Control-Allow-Methods:
                      type: "string"
                    Access-Control-Allow-Origin:
                      type: "string"
              x-amazon-apigateway-integration:
                type: "mock"
                requestTemplates:
                  application/json: "{\"statusCode\": 200}"
                responses:
                  default:
                    statusCode: "200"
                    responseParameters:
                      method.response.header.Access-Control-Allow-Headers: "'Content-Type,Authorization,X-Amz-Date,X-Api-Key,X-Amz-Security-Token'"
                      method.response.header.Access-Control-Allow-Methods: "'POST,OPTIONS'"
                      method.response.header.Access-Control-Allow-Origin: "'*'"
                    responseTemplates:
                      application/json: ""
                passthroughBehavior: "when_no_match"

          /materialidad/core/empaquetar-zip:
            post:
              summary: "Crear cliente de manera manual"
              security:
                - CognitoAuthorizer: []
              x-amazon-apigateway-integration:
                type: "aws_proxy"
                httpMethod: "POST"
                uri: !Sub 'arn:aws:apigateway:${AWS::Region}:lambda:path/2015-03-31/functions/${MaterialidadS3ZipPackerLambda.Arn}/invocations'
                passthroughBehavior: "when_no_match"
              responses:
                '201': { description: "Cliente creado." }
            options:
              summary: "CORS Metricas"
              consumes:
                - "application/json"
              produces:
                - "application/json"
              responses:
                '200':
                  description: "CORS Preflight exitoso"
                  headers:
                    Access-Control-Allow-Headers:
                      type: "string"
                    Access-Control-Allow-Methods:
                      type: "string"
                    Access-Control-Allow-Origin:
                      type: "string"
              x-amazon-apigateway-integration:
                type: "mock"
                requestTemplates:
                  application/json: "{\"statusCode\": 200}"
                responses:
                  default:
                    statusCode: "200"
                    responseParameters:
                      method.response.header.Access-Control-Allow-Headers: "'Content-Type,Authorization,X-Amz-Date,X-Api-Key,X-Amz-Security-Token'"
                      method.response.header.Access-Control-Allow-Methods: "'POST,OPTIONS'"
                      method.response.header.Access-Control-Allow-Origin: "'*'"
                    responseTemplates:
                      application/json: ""
                passthroughBehavior: "when_no_match"
          /notificaciones/historial:
            get:
              summary: "Consultar lista negra del sat"
              security:
                - CognitoAuthorizer: []
              x-amazon-apigateway-integration:
                type: "aws_proxy"
                httpMethod: "POST"
                uri: !Sub 'arn:aws:apigateway:${AWS::Region}:lambda:path/2015-03-31/functions/${NotificacionesHistorialLambda.Arn}/invocations'
                passthroughBehavior: "when_no_match"
              responses:
                '200': { description: "Estatus recuperado en 2ms." }
            options:
              summary: "CORS Metricas"
              consumes:
                - "application/json"
              produces:
                - "application/json"
              responses:
                '200':
                  description: "CORS Preflight exitoso"
                  headers:
                    Access-Control-Allow-Headers:
                      type: "string"
                    Access-Control-Allow-Methods:
                      type: "string"
                    Access-Control-Allow-Origin:
                      type: "string"
              x-amazon-apigateway-integration:
                type: "mock"
                requestTemplates:
                  application/json: "{\"statusCode\": 200}"
                responses:
                  default:
                    statusCode: "200"
                    responseParameters:
                      method.response.header.Access-Control-Allow-Headers: "'Content-Type,Authorization,X-Amz-Date,X-Api-Key,X-Amz-Security-Token'"
                      method.response.header.Access-Control-Allow-Methods: "'GET,OPTIONS'"
                      method.response.header.Access-Control-Allow-Origin: "'*'"
                    responseTemplates:
                      application/json: ""
                passthroughBehavior: "when_no_match"

          /notificaciones/leer:
            post:
              summary: "Crear cliente de manera manual"
              security:
                - CognitoAuthorizer: []
              x-amazon-apigateway-integration:
                type: "aws_proxy"
                httpMethod: "POST"
                uri: !Sub 'arn:aws:apigateway:${AWS::Region}:lambda:path/2015-03-31/functions/${NotificacionesModificarLambda.Arn}/invocations'
                passthroughBehavior: "when_no_match"
              responses:
                '201': { description: "Cliente creado." }
            options:
              summary: "CORS Metricas"
              consumes:
                - "application/json"
              produces:
                - "application/json"
              responses:
                '200':
                  description: "CORS Preflight exitoso"
                  headers:
                    Access-Control-Allow-Headers:
                      type: "string"
                    Access-Control-Allow-Methods:
                      type: "string"
                    Access-Control-Allow-Origin:
                      type: "string"
              x-amazon-apigateway-integration:
                type: "mock"
                requestTemplates:
                  application/json: "{\"statusCode\": 200}"
                responses:
                  default:
                    statusCode: "200"
                    responseParameters:
                      method.response.header.Access-Control-Allow-Headers: "'Content-Type,Authorization,X-Amz-Date,X-Api-Key,X-Amz-Security-Token'"
                      method.response.header.Access-Control-Allow-Methods: "'POST,OPTIONS'"
                      method.response.header.Access-Control-Allow-Origin: "'*'"
                    responseTemplates:
                      application/json: ""
                passthroughBehavior: "when_no_match"
          /materialidad/detonar:
            post:
              summary: "Crear cliente de manera manual"
              security:
                - CognitoAuthorizer: []
              x-amazon-apigateway-integration:
                type: "aws_proxy"
                httpMethod: "POST"
                uri: !Sub 'arn:aws:apigateway:${AWS::Region}:lambda:path/2015-03-31/functions/${MaterialidadDetonarLambda.Arn}/invocations'
                passthroughBehavior: "when_no_match"
              responses:
                '201': { description: "Cliente creado." }
            options:
              summary: "CORS Metricas"
              consumes:
                - "application/json"
              produces:
                - "application/json"
              responses:
                '200':
                  description: "CORS Preflight exitoso"
                  headers:
                    Access-Control-Allow-Headers:
                      type: "string"
                    Access-Control-Allow-Methods:
                      type: "string"
                    Access-Control-Allow-Origin:
                      type: "string"
              x-amazon-apigateway-integration:
                type: "mock"
                requestTemplates:
                  application/json: "{\"statusCode\": 200}"
                responses:
                  default:
                    statusCode: "200"
                    responseParameters:
                      method.response.header.Access-Control-Allow-Headers: "'Content-Type,Authorization,X-Amz-Date,X-Api-Key,X-Amz-Security-Token'"
                      method.response.header.Access-Control-Allow-Methods: "'POST,OPTIONS'"
                      method.response.header.Access-Control-Allow-Origin: "'*'"
                    responseTemplates:
                      application/json: ""
                passthroughBehavior: "when_no_match"
  # =========================================================================
  # RECURSOS DE COMPUTO (LAMBDA)
  # =====================================================================
  CargaExcelLambda:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub 'veritas-control-carga-excel-${Environment}'
      Handler: carga_excel_handler.handler
      CodeUri: ./lambdas/cargar_excel/
      Role: !GetAtt VeritasLambdaExecutionRole.Arn
      VpcConfig:
        SecurityGroupIds:
          - !Sub '{{resolve:ssm:/veritas-control/${Environment}/network/lambdas-sg-id}}'
        SubnetIds:
          - !Sub '{{resolve:ssm:/veritas-control/${Environment}/network/subnet-privada-1}}'
          - !Sub '{{resolve:ssm:/veritas-control/${Environment}/network/subnet-privada-2}}'
      Environment:
        Variables:
          DB_HOST: !Sub '{{resolve:ssm:/veritas-control/${Environment}/rds/postgres-endpoint}}'
          DB_PORT: '5432'
          DB_NAME: 'veritas_db'
          DB_USER: 'veritas_admin'
          DB_PASSWORD: !Sub '{{resolve:secretsmanager:${RdsSecretArnFromSsm}:SecretString:password}}'
          NOTIFICACIONES_TABLE: !Sub 'veritas-control-notificaciones-${Environment}'
      Policies:
        - Statement:
            - Effect: Allow
              Action:
                - "secretsmanager:GetSecretValue"
              Resource: !Sub 'arn:aws:secretsmanager:${AWS::Region}:${AWS::AccountId}:secret:veritas-control/dev/database*'
    Metadata:
      BuildMethod: python3.10

  ListaClientesLambda:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub 'veritas-control-lista-clientes-${Environment}'
      Handler: lista_clientes_handler.handler
      CodeUri: ./lambdas/lista_clientes/
      Role: !GetAtt VeritasLambdaExecutionRole.Arn
      VpcConfig:
        SecurityGroupIds:
          - !Sub '{{resolve:ssm:/veritas-control/${Environment}/network/lambdas-sg-id}}'
        SubnetIds:
          - !Sub '{{resolve:ssm:/veritas-control/${Environment}/network/subnet-privada-1}}'
          - !Sub '{{resolve:ssm:/veritas-control/${Environment}/network/subnet-privada-2}}'
      Environment:
        Variables:
          DB_HOST: !Sub '{{resolve:ssm:/veritas-control/${Environment}/rds/postgres-endpoint}}'
          DB_PORT: '5432'
          DB_NAME: 'veritas_db'
          DB_USER: 'veritas_admin'
          DB_PASSWORD: !Sub '{{resolve:secretsmanager:${RdsSecretArnFromSsm}:SecretString:password}}'
      Events:
        ApiEvent:
          Type: Api
          Properties:
            Path: /finanzas/clientes
            Method: get
            RestApiId: !Ref VeritasApi
    Metadata:
      BuildMethod: python3.10

  MetricasLambda:
    Type: AWS::Serverless::Function
    Metadata:
      BuildMethod: python3.10
    Properties:
      FunctionName: !Sub 'veritas-control-dashboard-metricas-${Environment}'
      Handler: metricas_handler.handler
      CodeUri: ./lambdas/metricas/
      Environment:
        Variables:
          DYNAMODB_TABLE: !Sub '{{resolve:ssm:/veritas-control/${Environment}/dynamo/finanzas-table-name}}'
      Policies:
        - DynamoDBReadPolicy:
            TableName: !Sub '{{resolve:ssm:/veritas-control/${Environment}/dynamo/finanzas-table-name}}'
      Events:
        ApiEvent:
          Type: Api
          Properties:
            Path: /finanzas/dashboard-metricas
            Method: get
            RestApiId: !Ref VeritasApi

  RegistroLambda:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub 'veritas-control-registro-handler-${Environment}'
      Handler: registro_handler.handler
      CodeUri: ./lambdas/registro/
      Environment:
        Variables:
          USER_POOL_ID: !Sub '{{resolve:ssm:/veritas-control/${Environment}/cognito/user-pool-id}}'
      Policies:
        - Statement:
            - Effect: Allow
              Action:
                - cognito-idp:AdminCreateUser
              Resource: !Ref CognitoUserPoolArn
      Events:
        ApiEvent:
          Type: Api
          Properties:
            Path: /registro
            Method: post
            RestApiId: !Ref VeritasApi
  
  CalcularKpisNoSqlLambda:
    Type: AWS::Serverless::Function
    Metadata:
      BuildMethod: python3.10
    Properties:
      FunctionName: !Sub 'veritas-control-calcular-kpis-nosql-${Environment}'
      Handler: calcular_kpis_nosql_handler.handler
      CodeUri: ./lambdas/calcular_kpis/
      Environment:
        Variables:
          DYNAMODB_TABLE: !Sub '{{resolve:ssm:/veritas-control/${Environment}/dynamo/finanzas-table-name}}'
      Policies:
        - DynamoDBCrudPolicy: { TableName: !Sub '{{resolve:ssm:/veritas-control/${Environment}/dynamo/finanzas-table-name}}' }
  
  GenerarUrlFirmadaLambda:
    Type: AWS::Serverless::Function
    Metadata:
      BuildMethod: python3.10
    Properties:
      FunctionName: !Sub 'veritas-control-solicitar-pase-s3-${Environment}'
      Handler: url_firmada_handler.handler
      CodeUri: ./lambdas/url_firmada/
      Environment:
        Variables:
          BUCKET_NAME: !Sub '{{resolve:ssm:/veritas-control/${Environment}/storage/facturas-bucket-name}}'
      Policies:
        - Statement:
            - Effect: Allow
              Action: [ "s3:PutObject" ]
              Resource:
                Fn::Join:
                  - ""
                  - - !Sub "{{resolve:ssm:/veritas-control/${Environment}/storage/facturas-bucket-arn}}"
                    - "/*"
      Events:
        ApiEvent:
          Type: Api
          Properties:
            Path: /finanzas/url-firmada
            Method: get
            RestApiId: !Ref VeritasApi

  ParserS3Lambda:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub 'veritas-control-v3-parser-s3-${Environment}'
      Handler: parser_s3_handler.handler
      CodeUri: ./lambdas/parser_s3/
      Policies:
        - S3ReadPolicy:
            BucketName: !Sub 'veritas-control-facturas-sat-${Environment}-${AWS::AccountId}'
    Metadata:
      BuildMethod: python3.10

  MaterialidadDisparadorLambda:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub 'veritas-control-mat-disparador-${Environment}'
      Handler: disparador_handler.handler
      CodeUri: ./lambdas/materialidad/
      Environment:
        Variables:
          STATE_MACHINE_ARN: !Ref VeritasMaterialidadStateMachine
      Policies:
        - Statement:
            - Effect: Allow
              Action: [ "states:StartExecution" ]
              Resource: !Ref VeritasMaterialidadStateMachine
      Events:
        ApiEvent:
          Type: Api
          Properties:
            Path: /materialidad/generar
            Method: post
            RestApiId: !Ref VeritasApi
    Metadata:
      BuildMethod: python3.10

  MaterialidadListarArchivosLambda:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub 'veritas-control-mat-listar-archivos-${Environment}'
      Handler: listar_archivos_handler.handler
      CodeUri: ./lambdas/materialidad/
      Environment:
        Variables:
          DYNAMODB_TABLE: !Sub '{{resolve:ssm:/veritas-control/${Environment}/dynamo/materialidad-table-name}}'
          BUCKET_NAME: !Sub '{{resolve:ssm:/veritas-control/${Environment}/storage/materialidad-bucket-name}}'
      Policies:
        # Permiso de lectura atómica sobre DynamoDB
        - DynamoDBReadPolicy: { TableName: !Sub '{{resolve:ssm:/veritas-control/${Environment}/dynamo/materialidad-table-name}}' }
        - S3ReadPolicy: { BucketName: !Sub '{{resolve:ssm:/veritas-control/${Environment}/storage/materialidad-bucket-name}}' }
      Events:
        ApiEvent:
          Type: Api
          Properties:
            Path: /materialidad/expediente
            Method: get
            RestApiId: !Ref VeritasApi
    Metadata:
      BuildMethod: python3.10

  MaterialidadContratoLambda:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub 'veritas-control-mat-contrato-${Environment}'
      Handler: contrato_handler.handler
      CodeUri: ./lambdas/materialidad/
      Environment:
        Variables:
          DYNAMODB_TABLE: !Sub '{{resolve:ssm:/veritas-control/${Environment}/dynamo/materialidad-table-name}}'
          BUCKET_NAME: !Sub '{{resolve:ssm:/veritas-control/${Environment}/storage/materialidad-bucket-name}}'
      Policies:
        - Statement:
            - Effect: Allow
              Action:
                - "bedrock:InvokeModel"
              Resource:
                - "arn:aws:bedrock:*:*:inference-profile/*"
                - "arn:aws:bedrock:*:*:foundation-model/*" 
            - Effect: Allow
              Action:
                - "dynamodb:PutItem"
                - "dynamodb:GetItem"
                - "dynamodb:UpdateItem"
                - "dynamodb:DeleteItem"
              Resource: !Sub 'arn:aws:dynamodb:${AWS::Region}:${AWS::AccountId}:table/veritas-control-materialidad-status-${Environment}'
            - Effect: Allow
              Action:
                - "s3:PutObject"
                - "s3:GetObject"
              Resource: !Sub 'arn:aws:s3:::veritas-control-materialidad-expedientes-${Environment}-${AWS::AccountId}/*'
    Metadata:
      BuildMethod: python3.10
  
  MaterialidadPlaneacionLambda:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub 'veritas-control-mat-planeacion-${Environment}'
      Handler: planeacion_handler.handler
      CodeUri: ./lambdas/materialidad/
      Policies:
        - Statement:
            - Effect: Allow
              Action:
                - "bedrock:InvokeModel"
              Resource: "arn:aws:bedrock:us-east-1::foundation-model/*"
    Metadata:
      BuildMethod: python3.10

  MaterialidadComunicacionLambda:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub 'veritas-control-mat-comunicacion-${Environment}'
      Handler: comunicacion_handler.handler
      CodeUri: ./lambdas/materialidad/
    Metadata:
      BuildMethod: python3.10

  MaterialidadEvidenciaLambda:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub 'veritas-control-mat-evidencia-${Environment}'
      Handler: evidencia_handler.handler
      CodeUri: ./lambdas/materialidad/
    Metadata:
      BuildMethod: python3.10

  MaterialidadConsolidadorLambda:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub 'veritas-control-mat-consolidador-${Environment}'
      Handler: consolidador_handler.handler
      CodeUri: ./lambdas/materialidad/
      Environment:
        Variables:
          DYNAMODB_TABLE: !Sub '{{resolve:ssm:/veritas-control/${Environment}/dynamo/materialidad-table-name}}'
      Policies:
        - DynamoDBCrudPolicy: { TableName: !Sub '{{resolve:ssm:/veritas-control/${Environment}/dynamo/materialidad-table-name}}' }
    Metadata:
      BuildMethod: python3.10

  MaterialidadPreventivoContratoLambda:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub 'veritas-control-mat-prev-contrato-${Environment}'
      Handler: preventivo_contrato_handler.handler
      CodeUri: ./lambdas/materialidad/
      Environment:
        Variables:
          DYNAMODB_TABLE: !Sub '{{resolve:ssm:/veritas-control/${Environment}/dynamo/materialidad-table-name}}'
          BUCKET_NAME: !Sub '{{resolve:ssm:/veritas-control/${Environment}/storage/materialidad-bucket-name}}'
      Policies:
        - Statement:
            - Effect: Allow
              Action: ["bedrock:InvokeModel"]
              Resource:
                - "arn:aws:bedrock:*:*:inference-profile/*"
                - "arn:aws:bedrock:*:*:foundation-model/*" 
        - S3CrudPolicy: 
            BucketName: !Sub 'veritas-control-materialidad-expedientes-${Environment}-${AWS::AccountId}'
        - DynamoDBCrudPolicy: { TableName: !Sub '{{resolve:ssm:/veritas-control/${Environment}/dynamo/materialidad-table-name}}' }
      Events:
        ApiEvent:
          Type: Api
          Properties: { Path: /materialidad/preventivo/contrato, Method: post, RestApiId: !Ref VeritasApi }
    Metadata:
      BuildMethod: python3.10

  AuditoriaListaNegraLambda:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub 'veritas-control-auditoria-69b-${Environment}'
      Handler: auditoria_69b_handler.handler
      CodeUri: ./lambdas/materialidad/
      Role: !GetAtt VeritasLambdaExecutionRole.Arn
      VpcConfig:
        SecurityGroupIds:
          - !Sub '{{resolve:ssm:/veritas-control/${Environment}/network/lambdas-sg-id}}'
        SubnetIds:
          - !Sub '{{resolve:ssm:/veritas-control/${Environment}/network/subnet-privada-1}}'
          - !Sub '{{resolve:ssm:/veritas-control/${Environment}/network/subnet-privada-2}}'
      Environment:
        Variables:
          DB_HOST: !Sub '{{resolve:ssm:/veritas-control/${Environment}/rds/postgres-endpoint}}'
          DB_PORT: '5432'
          DB_NAME: 'veritas_db'
          DB_USER: 'veritas_admin'
          DB_PASSWORD: !Sub '{{resolve:secretsmanager:${RdsSecretArnFromSsm}:SecretString:password}}'
      Events:
        ApiEvent:
          Type: Api
          Properties:
            Path: /finanzas/auditoria-sat
            Method: get
            RestApiId: !Ref VeritasApi
    Metadata:
      BuildMethod: python3.10
  
  LambdaDescargadoraSAT:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub 'veritas-descarga-lista-negra-csv-${Environment}'
      Handler: descargar_csv_lista_negra_handler.handler
      CodeUri: ./lambdas/archivo_lista_negra_sat/
      Policies:
        - S3CrudPolicy:
            BucketName: !Ref S3BucketTemporalSAT
      Environment:
        Variables:
          URL_CSV_SAT: "https://wu1agsprosta001.blob.core.windows.net/agsc-publicaciones/Datos_abiertos/Documents_AGAFF/Listado_completo_69-B.csv"
          BUCKET_DESTINO: !Ref S3BucketTemporalSAT
      Events:
        CronDiarioSAT:
          Type: Schedule
          Properties:
            Schedule: "cron(0 8 * * ? *)" # Corre a las 02:00 AM de México de forma automática
    Metadata:
      BuildMethod: python3.10

  LambdaInsertarListaNegraDB:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub 'veritas-insertar-lista-negra-${Environment}'
      Handler: insertar_lista_negra_handler.handler
      CodeUri: ./lambdas/insertar_lista_negra_sat/
      Role: !GetAtt VeritasLambdaExecutionRole.Arn
      VpcConfig:
        SecurityGroupIds:
          - !Sub '{{resolve:ssm:/veritas-control/${Environment}/network/lambdas-sg-id}}'
        SubnetIds:
          - !Sub '{{resolve:ssm:/veritas-control/${Environment}/network/subnet-privada-1}}'
          - !Sub '{{resolve:ssm:/veritas-control/${Environment}/network/subnet-privada-2}}'
      Environment:
        Variables:
          DB_HOST: !Sub '{{resolve:ssm:/veritas-control/${Environment}/rds/postgres-endpoint}}'
          DB_PORT: '5432'
          DB_NAME: 'veritas_db'
          DB_USER: 'veritas_admin'
          DB_PASSWORD: !Sub '{{resolve:secretsmanager:${RdsSecretArnFromSsm}:SecretString:password}}'
      Policies:
        - S3ReadPolicy:
            BucketName: !Ref S3BucketTemporalSAT
      Events:
        TriggerArchivoSAT:
          Type: S3
          Properties:
            Bucket: !Ref S3BucketTemporalSAT
            Events: s3:ObjectCreated:*
            Filter:
              S3Key:
                Rules:
                  - Name: suffix
                    Value: .csv
    Metadata:
      BuildMethod: python3.10

  CrearClienteManualLambda:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub 'veritas-control-cliente-manual-${Environment}'
      Handler: crear_cliente_manual_handler.handler
      CodeUri: ./lambdas/materialidad/
      Role: !GetAtt VeritasLambdaExecutionRole.Arn
      VpcConfig:
        SecurityGroupIds:
          - !Sub '{{resolve:ssm:/veritas-control/${Environment}/network/lambdas-sg-id}}'
        SubnetIds:
          - !Sub '{{resolve:ssm:/veritas-control/${Environment}/network/subnet-privada-1}}'
          - !Sub '{{resolve:ssm:/veritas-control/${Environment}/network/subnet-privada-2}}'
      Environment:
        Variables:
          DB_HOST: !Sub '{{resolve:ssm:/veritas-control/${Environment}/rds/postgres-endpoint}}'
          DB_PORT: '5432'
          DB_NAME: 'veritas_db'
          DB_USER: 'veritas_admin'
          DB_PASSWORD: !Sub '{{resolve:secretsmanager:${RdsSecretArnFromSsm}:SecretString:password}}'
          DYNAMODB_TABLE: !Sub 'veritas-control-materialidad-status-${Environment}'
      Architectures: 
        - x86_64
      Events:
        ApiEvent:
          Type: Api
          Properties:
            Path: /materialidad/cliente
            Method: post
            RestApiId: !Ref VeritasApi
    Metadata:
      BuildMethod: python3.10

  MaterialidadPreventivoPlaneacionLambda:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub 'veritas-control-mat-prev-planeacion-${Environment}'
      Handler: preventivo_planeacion_handler.handler
      CodeUri: ./lambdas/materialidad/
      Environment:
        Variables:
          DYNAMODB_TABLE: !Sub '{{resolve:ssm:/veritas-control/${Environment}/dynamo/materialidad-table-name}}'
          BUCKET_NAME: !Sub '{{resolve:ssm:/veritas-control/${Environment}/storage/materialidad-bucket-name}}'
      Policies:
        - Statement:
            - Effect: Allow
              Action: ["bedrock:InvokeModel"]
              Resource:
                - "arn:aws:bedrock:*:*:inference-profile/*"
                - "arn:aws:bedrock:*:*:foundation-model/*" 
        - S3CrudPolicy: { BucketName: !Sub '{{resolve:ssm:/veritas-control/${Environment}/storage/materialidad-bucket-name}}' }
        - DynamoDBCrudPolicy: { TableName: !Sub '{{resolve:ssm:/veritas-control/${Environment}/dynamo/materialidad-table-name}}' }
      Events:
        ApiEvent:
          Type: Api
          Properties: { Path: /materialidad/preventivo/planeacion, Method: post, RestApiId: !Ref VeritasApi }
    Metadata:
      BuildMethod: python3.10

  MaterialidadPreventivoBimestralLambda:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub 'veritas-control-mat-prev-bimestral-${Environment}'
      Handler: preventivo_bimestral_handler.handler
      CodeUri: ./lambdas/materialidad/
      Environment:
        Variables:
          DYNAMODB_TABLE: !Sub '{{resolve:ssm:/veritas-control/${Environment}/dynamo/materialidad-table-name}}'
          BUCKET_NAME: !Sub '{{resolve:ssm:/veritas-control/${Environment}/storage/materialidad-bucket-name}}'
      Policies:
        - Statement:
            - Effect: Allow
              Action: ["bedrock:InvokeModel"]
              Resource:
                - "arn:aws:bedrock:*:*:inference-profile/*"
                - "arn:aws:bedrock:*:*:foundation-model/*" 
        - S3CrudPolicy: { BucketName: !Sub '{{resolve:ssm:/veritas-control/${Environment}/storage/materialidad-bucket-name}}' }
        - DynamoDBCrudPolicy: { TableName: !Sub '{{resolve:ssm:/veritas-control/${Environment}/dynamo/materialidad-table-name}}' }
      Events:
        ApiEvent:
          Type: Api
          Properties: { Path: /materialidad/preventivo/bimestral, Method: post, RestApiId: !Ref VeritasApi }
    Metadata:
      BuildMethod: python3.10

  GuardarProductoNoSqlLambda:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub 'veritas-control-mat-nosql-producto-${Environment}'
      Handler: guardar_producto_nosql_handler.handler
      CodeUri: ./lambdas/materialidad/
      Environment:
        Variables:
          DYNAMODB_TABLE: !Sub '{{resolve:ssm:/veritas-control/${Environment}/dynamo/materialidad-table-name}}'
      Policies:
        - DynamoDBCrudPolicy: { TableName: !Sub '{{resolve:ssm:/veritas-control/${Environment}/dynamo/materialidad-table-name}}' }
      Events:
        ApiEvent:
          Type: Api
          Properties: { Path: /materialidad/cliente/producto, Method: post, RestApiId: !Ref VeritasApi }
    Metadata:
      BuildMethod: python3.10

  MaterialidadComunicacionEmpresarialLambda:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub 'veritas-control-mat-prev-comunicacion-${Environment}'
      Handler: preventivo_comunicacion_handler.handler
      CodeUri: ./lambdas/materialidad/
      Environment:
        Variables:
          DYNAMODB_TABLE: !Sub '{{resolve:ssm:/veritas-control/${Environment}/dynamo/materialidad-table-name}}'
          BUCKET_NAME: !Sub '{{resolve:ssm:/veritas-control/${Environment}/storage/materialidad-bucket-name}}'
      Policies:
        - Statement:
            - Effect: Allow
              Action: ["bedrock:InvokeModel"]
              Resource:
                - "arn:aws:bedrock:*:*:inference-profile/*"
                - "arn:aws:bedrock:*:*:foundation-model/*"
        - S3CrudPolicy: { BucketName: !Sub '{{resolve:ssm:/veritas-control/${Environment}/storage/materialidad-bucket-name}}' }
        - DynamoDBCrudPolicy: { TableName: !Sub '{{resolve:ssm:/veritas-control/${Environment}/dynamo/materialidad-table-name}}' }
      Events:
        ApiEvent:
          Type: Api
          Properties: { Path: /materialidad/preventivo/comunicacion, Method: post, RestApiId: !Ref VeritasApi }
    Metadata:
      BuildMethod: python3.10

  MaterialidadEvidenciaDigitalLambda:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub 'veritas-control-mat-prev-evidencia-${Environment}'
      Handler: preventivo_evidencia_handler.handler
      CodeUri: ./lambdas/materialidad/
      Environment:
        Variables:
          DYNAMODB_TABLE: !Sub '{{resolve:ssm:/veritas-control/${Environment}/dynamo/materialidad-table-name}}'
          BUCKET_NAME: !Sub '{{resolve:ssm:/veritas-control/${Environment}/storage/materialidad-bucket-name}}'
      Policies:
        - S3CrudPolicy: { BucketName: !Sub '{{resolve:ssm:/veritas-control/${Environment}/storage/materialidad-bucket-name}}' }
        - DynamoDBCrudPolicy: { TableName: !Sub '{{resolve:ssm:/veritas-control/${Environment}/dynamo/materialidad-table-name}}' }
      Events:
        ApiEvent:
          Type: Api
          Properties: { Path: /materialidad/preventivo/evidencia, Method: post, RestApiId: !Ref VeritasApi }
    Metadata:
      BuildMethod: python3.10

  VeritasLambdaExecutionRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: !Sub 'veritas-control-lambda-execution-role-${Environment}'
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal:
              Service: [lambda.amazonaws.com]
            Action: ["sts:AssumeRole"]
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/service-role/AWSLambdaBasicExecutionRole
        - arn:aws:iam::aws:policy/AmazonDynamoDBFullAccess
        - arn:aws:iam::aws:policy/AmazonS3FullAccess
        - arn:aws:iam::aws:policy/service-role/AWSLambdaVPCAccessExecutionRole
  
  VeritasStepFunctionsExecutionRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: !Sub 'veritas-control-stepfunctions-role-${Environment}'
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal:
              Service: 
                - states.amazonaws.com
            Action: 
              - "sts:AssumeRole"
      Policies:
        - PolicyName: !Sub 'veritas-control-stepfunctions-policy-${Environment}'
          PolicyDocument:
            Version: "2012-10-17"
            Statement:
              - Effect: Allow
                Action:
                  - lambda:InvokeFunction
                Resource:
                  - !GetAtt MaterialidadExtractorPostgresLambda.Arn
                  - !GetAtt MaterialidadRedactorBedrockLambda.Arn
                  - !GetAtt MaterialidadEscultorReportLabLambda.Arn

  VeritasMaterialidadStateMachine:
    Type: AWS::Serverless::StateMachine
    Properties:
      Name: !Sub 'veritas-control-materialidad-orquestador-${Environment}'
      Definition:
        Comment: "Orquestador asincrono pericial para generacion masiva de PDFs con IA"
        StartAt: ProcesamientoParaleloMaterialidad
        States:
          ProcesamientoParaleloMaterialidad:
            Type: Parallel
            Next: ConsolidarExpedienteFinal
            Branches:
              - StartAt: GenerarFaseContrato
                States:
                  GenerarFaseContrato:
                    Type: Task
                    Resource: !GetAtt MaterialidadContratoLambda.Arn
                    End: true
              - StartAt: GenerarFasePlaneacion
                States:
                  GenerarFasePlaneacion:
                    Type: Task
                    Resource: !GetAtt MaterialidadPlaneacionLambda.Arn
                    End: true
              - StartAt: GenerarFaseComunicacion
                States:
                  GenerarFaseComunicacion:
                    Type: Task
                    Resource: !GetAtt MaterialidadComunicacionLambda.Arn
                    End: true
              - StartAt: GenerarFaseEvidenciaFacturas
                States:
                  GenerarFaseEvidenciaFacturas:
                    Type: Task
                    Resource: !GetAtt MaterialidadEvidenciaLambda.Arn
                    End: true
          ConsolidarExpedienteFinal:
            Type: Task
            Resource: !GetAtt MaterialidadConsolidadorLambda.Arn
            End: true
      Policies:
        - LambdaInvokePolicy: { FunctionName: !Ref MaterialidadContratoLambda }
        - LambdaInvokePolicy: { FunctionName: !Ref MaterialidadPlaneacionLambda }
        - LambdaInvokePolicy: { FunctionName: !Ref MaterialidadComunicacionLambda }
        - LambdaInvokePolicy: { FunctionName: !Ref MaterialidadEvidenciaLambda }
        - LambdaInvokePolicy: { FunctionName: !Ref MaterialidadConsolidadorLambda }

  VeritasProcesadorS3StateMachine:
    Type: AWS::Serverless::StateMachine
    Properties:
      Name: !Sub 'veritas-control-s3-procesador-${Environment}'
      Definition:
        Comment: "Orquestador de eventos contables disparado por S3"
        StartAt: ParsearDocumentoS3
        States:
          ParsearDocumentoS3:
            Type: Task
            Resource: !GetAtt ParserS3Lambda.Arn
            Next: InyeccionParalelaCQRS
          InyeccionParalelaCQRS:
            Type: Parallel
            End: true
            Branches:
              - StartAt: GuardarPostgresCrudo
                States:
                  GuardarPostgresCrudo:
                    Type: Task
                    Resource: !GetAtt CargaExcelLambda.Arn
                    TimeoutSeconds: 40
                    End: true
              - StartAt: CalcularKPIsNoSql
                States:
                  CalcularKPIsNoSql:
                    Type: Task
                    Resource: !GetAtt CalcularKpisNoSqlLambda.Arn
                    End: true
      Policies:
        - LambdaInvokePolicy: { FunctionName: !Ref ParserS3Lambda }
        - LambdaInvokePolicy: { FunctionName: !Ref CargaExcelLambda }
        - LambdaInvokePolicy: { FunctionName: !Ref CalcularKpisNoSqlLambda }
      Events:
        FacturaSubidaS3Event:
          Type: EventBridgeRule
          Properties:
            EventBusName: default
            Pattern:
              source: ["aws.s3"]
              detail-type: ["Object Created"]
              detail:
                bucket:
                  name: [ !Sub '{{resolve:ssm:/veritas-control/${Environment}/storage/facturas-bucket-name}}' ]

  MaterialidadOrquestadorLambda:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub 'veritas-control-mat-orquestador-${Environment}'
      Runtime: python3.10
      Handler: orquestador_materialidad.handler
      CodeUri: ./lambdas/materialidad/
      Environment:
        Variables:
          DYNAMODB_TABLE: !Sub 'veritas-control-materialidad-status-${Environment}'
          BUCKET_NAME: '{{resolve:ssm:/veritas-control/dev/storage/materialidad-bucket-name}}'
          # 🚀 ENRIQUECEMOS EL ENTORNO: Pasamos el ARN de la Step Function que va a despertar
          STATE_MACHINE_ARN: !Ref VeritasMaterialidadStateMachine
      Policies:
        - Statement:
            # 🎯 REPARACIÓN REINA: Reemplazamos la mala práctica de invocación directa
            # por el disparador asíncronio oficial de la Máquina de Estados de Veritas
            - Effect: Allow
              Action: ["states:StartExecution"]
              Resource: !Ref VeritasMaterialidadStateMachine
            
            # Formato de ARN completo reglamentario exigido por las aduanas de AWS IAM
            - Effect: Allow
              Action: 
                - "dynamodb:PutItem"
                - "dynamodb:UpdateItem"
                - "dynamodb:GetItem"
              Resource: !Sub 'arn:aws:dynamodb:${AWS::Region}:${AWS::AccountId}:table/veritas-control-materialidad-status-${Environment}'
              
        - AWSLambdaVPCAccessExecutionRole

  VeritasMaterialidadQueue:
    Type: AWS::SQS::Queue
    Properties:
      QueueName: !Sub 'veritas-control-mat-queue-${Environment}'
      VisibilityTimeout: 300

  MaterialidadExtractorPostgresLambda:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub 'veritas-control-mat-step1-extractor-${Environment}'
      Handler: step1_extractor_handler.handler
      CodeUri: ./lambdas/materialidad/
      VpcConfig:
        SecurityGroupIds:
          - !Sub '{{resolve:ssm:/veritas-control/${Environment}/network/lambdas-sg-id}}'
        SubnetIds:
          - !Sub '{{resolve:ssm:/veritas-control/${Environment}/network/subnet-privada-1}}'
          - !Sub '{{resolve:ssm:/veritas-control/${Environment}/network/subnet-privada-2}}'
      Environment:
        Variables:
          DB_HOST: !Sub '{{resolve:ssm:/veritas-control/${Environment}/rds/postgres-endpoint}}'
          DB_PORT: '5432'
          DB_NAME: 'veritas_db'
          DB_USER: 'veritas_admin'
          DB_PASSWORD: !Sub '{{resolve:secretsmanager:${RdsSecretArnFromSsm}:SecretString:password}}'
          DYNAMODB_TABLE: !Sub 'veritas-control-materialidad-status-${Environment}'
      Policies:
        - AWSLambdaVPCAccessExecutionRole
        - Statement:
            - Effect: Allow
              Action: ["dynamodb:GetItem", "dynamodb:UpdateItem"]
              Resource: !Sub 'arn:aws:dynamodb:${AWS::Region}:${AWS::AccountId}:table/veritas-control-materialidad-status-${Environment}'
    Metadata:
      BuildMethod: python3.11

  MaterialidadRedactorBedrockLambda:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub 'veritas-control-mat-step2-redactor-${Environment}'
      Handler: step2_redactor_handler.handler
      CodeUri: ./lambdas/materialidad/
      Policies:
        - Statement:
            - Effect: Allow
              Action: ["bedrock:InvokeModel"]
              Resource: ["arn:aws:bedrock:*:*:inference-profile/*", "arn:aws:bedrock:*:*:foundation-model/*"]
    Metadata:
      BuildMethod: python3.11

  MaterialidadEscultorReportLabLambda:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub 'veritas-control-mat-step3-escultor-${Environment}'
      Handler: step3_escultor_handler.handler
      CodeUri: ./lambdas/materialidad/
      Environment:
        Variables:
          DYNAMODB_TABLE: !Sub 'veritas-control-materialidad-status-${Environment}'
          BUCKET_NAME: !Sub 'veritas-control-materialidad-expedientes-${Environment}-${AWS::AccountId}'
      Policies:
        # 🚀 SOLUCIÓN REINA ESTRATÉGICA: Colocamos el nombre de bucket nominal estático dev de SAM
        # Esto es 100% legal para S3CrudPolicy y disuelve el error de "No export found" en 2ms
        - S3CrudPolicy:
            BucketName: !Sub 'veritas-control-materialidad-expedientes-${Environment}-${AWS::AccountId}'
        - Statement:
            - Effect: Allow
              Action: ["dynamodb:UpdateItem"]
              Resource: !Sub 'arn:aws:dynamodb:${AWS::Region}:${AWS::AccountId}:table/veritas-control-materialidad-status-${Environment}'
    Metadata:
      BuildMethod: python3.11

  VeritasMaterialidadStateMachine:
    Type: AWS::StepFunctions::StateMachine
    Properties:
      StateMachineName: !Sub 'veritas-materialidad-pipeline-${Environment}'
      RoleArn: !GetAtt VeritasStepFunctionsExecutionRole.Arn
      DefinitionString: !Sub |
        {
          "Comment": "Pipeline Forense de Materialidad - Veritas Control",
          "StartAt": "ExtraerDatos",
          "States": {
            "ExtraerDatos": {
              "Type": "Task",
              "Resource": "arn:aws:lambda:us-east-1:049255850526:function:veritas-control-mat-step1-extractor-dev",
              "ResultPath": "$.step1_output",
              "Next": "GenerarTextoIA"
            },
            "GenerarTextoIA": {
              "Type": "Task",
              "Resource": "arn:aws:lambda:us-east-1:049255850526:function:veritas-control-mat-step2-redactor-dev",
              "ResultPath": "$.step2_output",
              "Next": "EsculpirPDF"
            },
            "EsculpirPDF": {
              "Type": "Task",
              "Resource": "arn:aws:lambda:us-east-1:049255850526:function:veritas-control-mat-step3-escultor-dev",
              "ResultPath": "$.step3_output",
              "End": true
            }
          }
        }

  MaterialidadS3ZipPackerLambda:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub 'veritas-control-mat-core-zip-packer-${Environment}'
      Handler: core_zip_packer_handler.handler
      CodeUri: ./lambdas/materialidad/
      Timeout: 120
      MemorySize: 512
      Policies:
        # 🚀 SOLUCIÓN REINA ESTRATÉGICA: S3CrudPolicy unificado con nomenclatura nativa de SAM
        - S3CrudPolicy:
            BucketName: !Sub 'veritas-control-materialidad-expedientes-${Environment}-${AWS::AccountId}'
  
  NotificacionesHistorialLambda:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub 'veritas-control-notif-historial-${Environment}'
      Handler: historial_handler.handler
      CodeUri: ./lambdas/notificaciones/
      Role: !GetAtt VeritasLambdaExecutionRole.Arn
      Environment:
        Variables:
          NOTIFICACIONES_TABLE: !Sub 'veritas-control-notificaciones-${Environment}'
      Events:
        ApiEvent:
          Type: Api
          Properties:
            Path: /notificaciones/historial
            Method: get
            RestApiId: !Ref VeritasApi
    Metadata:
      BuildMethod: python3.10

  NotificacionesModificarLambda:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub 'veritas-control-notif-leer-${Environment}'
      Handler: leer_handler.handler
      CodeUri: ./lambdas/notificaciones/
      Role: !GetAtt VeritasLambdaExecutionRole.Arn
      Environment:
        Variables:
          NOTIFICACIONES_TABLE: !Sub '/veritas-control/${Environment}/dynamo/notificaciones-table-name'
      Events:
        ApiEvent:
          Type: Api
          Properties:
            Path: /notificaciones/leer
            Method: post
            RestApiId: !Ref VeritasApi
    Metadata:
      BuildMethod: python3.10

  MaterialidadDetonarLambda:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub 'veritas-control-materialidad-detonar-${Environment}'
      Handler: detonar_handler.handler
      CodeUri: ./lambdas/materialidad/
      Role: !GetAtt VeritasLambdaExecutionRole.Arn
      # 🚀 VELOCIDAD CRÍTICA: Se omite VpcConfig de forma deliberada para responder en 0.1ms plans
      # y conectarse directamente con la red global de Bedrock, DynamoDB y Step Functions sin NAT Gateways.
      Environment:
        Variables:
          # 🔌 Llavero de variables inmutables de red resueltas por SAM:
          NOTIFICACIONES_TABLE: !Sub 'veritas-control-materialidad-status-${Environment}'
          MATERIALIDAD_STATE_MACHINE_ARN: !Ref PipelineMaterialidadStateMachine
      Events:
        ApiEvent:
          Type: Api
          Properties:
            Path: /materialidad/detonar
            Method: post
            RestApiId: !Ref VeritasApi
    Metadata:
      BuildMethod: python3.10

  S3BucketTemporalSAT:
    Type: AWS::S3::Bucket
    Properties:
      BucketName: !Sub "veritas-sat-listas-negras-temporal-${AWS::AccountId}"
